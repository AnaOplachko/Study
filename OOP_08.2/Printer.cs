using System.Linq.Expressions;

/*namespace OOP_08._2;

public class Printer
{ 
    string stroka = null;
    string color = null;

    public Printer(string stroka, string color)
    {
        this.stroka = stroka;
        this.color = color;
    }

    object element = Enum.Parse(typeof(Colors), color);

    Colors number = (Colors)element;
    
    public void Print()
    {
        Console.ForegroundColor = Colors.Gray;
        Console.WriteLine(stroka);
    }
    
}*/